{
  "name": "seed",
  "version": "0.1.0",
  "description": "Serverless API with lambda function and netlify CI/CD",
  "main": "src/api.js",
  "scripts": {
    "start": "nodemon -r dotenv/config",
    "serve": "netlify-lambda serve src --config ./webpack.functions.js",
    "build": "netlify-lambda build src"
  },
  "dependencies": {
    "express": "^4.17.1",
    "faunadb": "^4.1.3",
    "netlify-lambda": "^2.0.3",
    "nodemon": "^2.0.7",
    "serverless-http": "^2.7.0"
  },
  "devDependencies": {
    "dotenv-webpack": "^7.0.2"
  }
}
